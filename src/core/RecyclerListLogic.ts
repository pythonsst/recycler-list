// JS side remains thin.
// All heavy logic lives in Nitro + native code.
export function explainRecyclerList() {
  return 'RecyclerList delegates scrolling, windowing, and recycling to native code.';
}
